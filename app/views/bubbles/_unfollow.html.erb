<%= form_for(current_user.UserBubbleRelations.find_by_bubble_id(bubble.id),
             html: { method: :delete }) do |f| %>
  <%= f.submit "Unfollow", class: "medium button radius secondary" %>
<% end %>