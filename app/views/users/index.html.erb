<% provide(:title, 'All users') %>
<h1>All users</h1>


<ul  class="users">	
  <div id= "results">
  	<%= render @users %>
  </div>
</ul>
<%= will_paginate @users %>
<!-- <%= pageless(@users.total_pages, users_path, "#results") %> -->


